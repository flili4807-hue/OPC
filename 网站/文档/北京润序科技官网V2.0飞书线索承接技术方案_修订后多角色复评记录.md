# 北京润序科技官网 V2.0 飞书线索承接技术方案修订后多角色复评记录

日期：2026-07-14  
评审对象：`北京润序科技官网V2.0飞书线索承接技术方案.md`  
对应 PRD：`北京润序科技官网V2.0飞书线索承接PRD.md`  
对应设计：`北京润序科技官网V2.0飞书线索承接UIUX设计方案.md`

评审角色：

- 业务产品经理
- 售前
- 产品负责人
- UI/UX 设计
- 前端研发
- 后端研发
- 测试
- 内容 / 品牌负责人
- 创始人本人
- 技术负责人 / 架构

## 1. 总体结论

结论：通过，可进入开发。

本次 P1 修订已完成：

1. 飞书卡片收敛为 Webhook 文本消息或简单卡片。
2. 补充飞书企业自建应用和多维表权限前置要求。
3. SQLite migration 增加幂等要求。
4. CSV 导出增加 `notify_bitable`、`bitable_record_id`、`bitable_error`。
5. 补录脚本增加 `--dry-run`、`--limit`、`--status`。
6. 增加真实发送灰度流程。
7. 明确 `tenant_access_token` 内存缓存。
8. 飞书卡片和多维表写入支持独立启停。

方案已具备进入 V2.0-MVP 开发的条件。

## 2. 分角色复评意见

### 2.1 业务产品经理

结论：通过。

通过点：

- CSV 可识别多维表未写入线索，业务可兜底补录。
- SQLite 事实源口径清楚。
- 飞书卡片和多维表可分步启用，降低上线风险。

保留建议：

- P2：上线后用一周数据判断是否需要增加“首次跟进时间”。

### 2.2 售前

结论：通过。

通过点：

- MVP 使用简单卡片，手机端更稳定。
- 卡片首屏信息仍保留联系方式和建议首问问题。
- 多维表写入失败不影响本地数据，跟进可通过 CSV 兜底。

保留建议：

- P2：真实飞书卡片上线前用手机端截图确认可读性。

### 2.3 产品负责人

结论：通过。

通过点：

- 灰度流程清楚：先 mock，再真实卡片，再测试表，最后正式表。
- 卡片和多维表独立启停，避免外部依赖拖慢上线。
- V2.1 能力没有混入 V2.0。

保留建议：

- P2：开发任务中继续将“卡片按钮更新状态”标记为 V2.1。

### 2.4 UI/UX 设计

结论：通过。

通过点：

- 简单卡片符合首屏可读性。
- 字段截断和空值规则可实现。
- 不影响官网用户体验。

保留建议：

- P2：真实卡片首版截图需要回传给设计确认。

### 2.5 前端研发

结论：通过。

通过点：

- 前端不需要新增展示逻辑。
- `notifyStatus` 继续不展示给用户。
- 后端通知超时不应显著增加前端等待。

保留建议：

- P2：如果后端通知耗时仍影响提交体验，后续改异步队列。

### 2.6 后端研发

结论：通过。

通过点：

- migration 幂等要求明确。
- token 缓存明确。
- 字段映射配置明确。
- 补录脚本参数明确。
- 卡片和多维表独立启停，便于联调。

开发注意：

- migration 需要在服务启动时执行。
- `retry-bitable.js --dry-run` 必须默认不写数据。
- 日志不得输出 token、完整联系方式和完整项目背景。

### 2.7 测试

结论：通过。

通过点：

- Mock、未配置、失败、真实灰度均有测试路径。
- 补录脚本支持 dry-run 和 limit，降低误操作风险。
- 权限检查清单可执行。

测试重点：

1. `FEISHU_CARD_ENABLED=false`。
2. `BITABLE_ENABLED=false`。
3. `FEISHU_CARD_MOCK=true`。
4. `BITABLE_MOCK=true`。
5. `retry-bitable.js --dry-run`。
6. `retry-bitable.js --limit 1`。

### 2.8 内容 / 品牌负责人

结论：通过。

通过点：

- 保留“自评诊断”语境。
- 没有使用官方认证或正式评估等风险表达。
- 简单卡片降低信息噪音。

保留建议：

- P2：建议动作继续使用“建议优先确认”。

### 2.9 创始人本人

结论：通过。

通过点：

- 可以先收飞书卡片，再逐步接多维表。
- 权限检查项明确。
- CSV 仍能兜底查看线索。

上线前需确认：

1. 飞书群成员。
2. 多维表访问权限。
3. 是否先用测试表联调。

### 2.10 技术负责人 / 架构

结论：通过。

通过点：

- 上线优先级清晰：SQLite > 邮件 > 飞书卡片 > 多维表。
- 外部依赖可独立启停。
- token 缓存、字段映射、补录脚本、权限检查已覆盖。

保留建议：

- P2：后续如果线索量增加，通知和多维表写入改为异步队列。

## 3. 开发准入结论

准入结论：通过。

可以进入 V2.0 飞书线索承接开发。

开发顺序建议：

1. SQLite 字段 migration。
2. `.env.example` 增加飞书卡片和多维表配置。
3. `config/bitable-field-map.json`。
4. 飞书卡片 mock / 简单卡片真实发送。
5. 多维表 mock。
6. 多维表真实写入。
7. CSV 导出增强。
8. `scripts/retry-bitable.js`。
9. 权限检查清单。
10. Mock / 真实灰度测试。
